<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Warehouse Worker Routes
|--------------------------------------------------------------------------
|
| Routes for warehouse workers to manage daily operations like transfers,
| inventory updates, and barcode scanning
| Prefix: /warehouse
| Middleware: auth, check.roles.permissions:warehouse
|
*/

// TODO(claude): Add warehouse worker routes here as controllers are migrated
// Example routes structure:
// Route::get('dashboard', [DashboardController::class, 'index'])->name('dashboard');
// Route::resource('transfer', TransferController::class);
// Route::resource('locations', LocationsController::class);
