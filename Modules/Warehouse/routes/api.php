<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| API routes for warehouse operations (future use)
| Prefix: /api/warehouse
| Middleware: api, throttle:60,1
|
*/

// TODO(claude): Add API routes here in future phases
// Example routes structure:
// Route::apiResource('warehouses', WarehouseApiController::class);
// Route::apiResource('inventory', InventoryApiController::class);
